<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="mySpinner" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse" />

    <style name="textPrimary" parent="@android:style/Widget.TextView">
    <item name="android:textColor">#303030</item>
        <item name="android:textSize">15sp</item>
        <item name="android:ellipsize">end</item>
    </style>
    <style name="textSecondary" parent="@android:style/Widget.TextView">
        <item name="android:textColor">#595959</item>
        <item name="android:textSize">13sp</item>
        <item name="android:ellipsize">end</item>
    </style>
    <style name="textTertiary" parent="@android:style/Widget.TextView">
        <item name="android:textColor">#595959</item>
        <item name="android:textSize">11sp</item>
        <item name="android:ellipsize">end</item>
    </style>


</resources>
